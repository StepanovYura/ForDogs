// Показывается мгновенно, пока сервер готовит страницу. Без этого переход
// по ссылке выглядит как зависание: браузер ждёт ответ, а на экране ничего
// не меняется. Заодно делает предзагрузку ссылок дешёвой — Next.js
// подгружает только этот каркас, а не весь рендер с запросами в базу.
export default function Loading() {
  return (
    <div className="page section" aria-busy="true">
      <div className="caption">Загружаем…</div>
    </div>
  )
}
